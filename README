# HA proxy demo
Quick local HA proxy demo using docker-compose

# Usage
docker-compose up --build -d
Open a browser and visit http://127.0.0.1 - Requests should be routed to the backend servers!

# Load test script
Sends X amount of concurrent requests over Y amount of total requests, then calculates statistics and plots a visualization chart
python3 loadtest.py
